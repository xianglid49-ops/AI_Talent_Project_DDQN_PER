# Reviewer Traceability

This file maps the principal editor/reviewer concerns to concrete reproducibility controls in the repository.

| Concern | Repository response | Primary files / artifacts |
|---|---|---|
| Custom computational pipeline must be available | Complete flat implementation of preprocessing, simulation, GNN–Transformer, DDQN-PER, baselines, evaluation, and explainability | All source files; `CODE_AVAILABILITY.md` |
| Dataset URL/provenance must be verifiable | Supports the public Kaggle slug and local immutable CSV input; writes source metadata and discovered schema | `data_pipeline.py`; generated `provenance.json` |
| RL algorithm inconsistent (Meta-RL/PPO/MAML/DDQN) | Only **DDQN with PER** is implemented as the adaptive RL algorithm; no PPO/MAML code is present | `models.py`, `config.yaml` |
| Dataset does not contain resumes, teams, collaboration, availability, behavior, outcomes, or longitudinal transitions | Dataset-derived and simulation-generated variables are separated in code and provenance | `data_pipeline.py`, `simulation.py`, `provenance.json` |
| Heuristic labels risk circular evaluation | Label formula is explicit, configurable, dataset-derived only, and excludes simulated availability/behavior/collaboration variables | `data_pipeline.py`, `config.yaml`, generated `label_definition.json` |
| 5-fold CV conflicts with 70/15/15 split | Uses 5-fold stratified CV. Each 80% development fold is internally split, with 15% of development reserved for validation; 20% remains untouched test | `run_experiment.py`, generated `split_manifest.csv` |
| Results inconsistent across tables/abstract | All methods are evaluated on identical fold/seed memberships; results are regenerated rather than hard-coded | `run_experiment.py`, `evaluation.py`, `fold_metrics.csv`, `summary_metrics.csv` |
| Full proposed method omitted from baseline comparison | Unified output contains Rule-based, TF-IDF, Transformer, GNN-only, and Proposed GNN–Transformer–DDQN-PER | `fold_metrics.csv`, `summary_metrics.csv` |
| Explainability method under-specified | SHAP uses up to 500 validation background samples and explains only the final prediction head with fixed graph/Transformer latent inputs | `evaluation.py`, `shap_feature_importance.csv` |
| GNN/Transformer sections contain unrelated supply-chain/navigation concepts | Repository domain objects are candidate, project, skill, assignment, compatibility, and simulated workforce signals only | `models.py`, `simulation.py` |
| Unsupported technical claims | Code makes no claim that the architecture “solves” vanishing gradients or is categorically superior; residual/normalization are simply implementation choices | `models.py`, `README.md` |
| Statistical robustness | Multiple seeds, mean±SD, 95% confidence intervals, and paired t-tests are generated from held-out runs | `evaluation.py`, `summary_metrics.csv`, `paired_tests.csv` |

## Important manuscript reconciliation items

The current manuscript text still contains legacy passages that are inconsistent with the reviewer response. Before resubmission, the manuscript should be synchronized to the repository in at least the following places:

1. Replace remaining “Meta-Reinforcement Learning,” PPO, MAML, and meta-policy implementation claims with the implemented **DDQN-PER adaptive reinforcement-learning module**.
2. Remove supply-chain route optimization, target navigation, path-length loss, and unrelated agent-navigation examples.
3. Remove the standalone 70/15/15 split description; retain one precise 5-fold protocol.
4. Replace any result table whose values were not generated by the final reproducibility run.
5. State clearly that synthetic dynamic variables are controlled simulation variables.
6. State clearly that heuristic labels are not observed hiring/project-success outcomes.
7. Ensure the SHAP section says that fixed latent features supplied to the final prediction head are explained, not internal graph message passing.
8. Add the permanent archival software citation to the manuscript only after the exact tagged release used for the final experiments has been archived.
